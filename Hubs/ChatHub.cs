using Microsoft.AspNetCore.SignalR;

namespace ITE5331FinalProject.Hubs
{
    public class ChatHub : Hub
    {
        public async Task SendMessage(string message)
        {
            await Clients.All.SendAsync("ReceiveMessage", message);
        }
    }
}
